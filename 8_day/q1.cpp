#include <bits/stdc++.h>
using namespace std;

void rec(int x, int y){
    if(x == y){
        return;
    }
    cout << x << " ";
    x++;
    rec(x, y);
}


int main(){
    rec(1, 11);
    i <= s.size()
}
