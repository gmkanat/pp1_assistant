#include <bits/stdc++.h>
using namespace std;

int main(){
    int n, m;
    cin >> n >> m;
    vector<int> v(n), c(m);
    for(int i = 0; i < n; i++){
        cin >> v[i];
    }
    for(int i = 0; i < m; i++){
        cin >> c[i];
    }
    vector<int> v1, c1;
    for(int i = 0; i < n; i++){
        if(v[i] )
    }
}
